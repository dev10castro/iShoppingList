<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IShoppingList_evaluable"
        tools:targetApi="31">

        <!-- Splash Activity -->
        <activity
            android:name=".ActivitySplash"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity
            android:name=".Edit_Activity"
            android:exported="false" />

        <activity
            android:name=".Add_New_Product"
            android:exported="false" />

        <activity
            android:name=".Activity_detaill"
            android:exported="false" />

        <activity
            android:name=".Spinner_activity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false">
            <!-- MainActivity should not have LAUNCHER category -->
        </activity>
    </application>

</manifest>
